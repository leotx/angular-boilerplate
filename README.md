# Angular Boilerplate!

This app uses Angular and Semantic UI

## How to Install

* Install the client-side dependencies:

  ```shell
  npm install
  ```

  ```shell
  bower install
  ```
  
* To run dev server:
  
   ```shell
  gulp dev
  ```
